[tool-quirk] better-sqlite3 native binding missing error ("could not locate the bindings file" / "session indexing failed") in pi-hermes-memory extension. Cause: package installed without build step — `build/` and `prebuilds/` dirs absent under `node_modules/.pnpm/better-sqlite3@<ver>/node_modules/better-sqlite3/`. Fix: run `npm run install` (triggers `prebuild-install || node-gyp rebuild --release`) in that package dir, produces `build/Release/better_sqlite3.node`. Verify with `node -e "require('better-sqlite3')(':memory:')"`. Prevention: ensure pnpm `onlyBuiltDependencies` / `.npmrc` allows build scripts for better-sqlite3. Path root here was `/root/node_modules/.pnpm/...` (pnpm global store at /root). <!-- created=2026-06-21, last=2026-06-21 -->
§
[tool-quirk] Astro + Svelte 5 dev hydration error `TypeError: node.remove is not a function` + `Cannot read properties of undefined (reading 'call')` at `get_first_child` (chunk-3XM65Y6W.js ~line 4867, `first_child_getter.call(node)`). Cause: stale/corrupted Vite dep cache + HMR desync, NOT a code bug. Signal: `from_html` takes non-hydrating path during hydrate + stale `?v=XXXX` chunk hash in browser. Fix: stop dev server, `rm -rf node_modules/.vite .astro node_modules/.astro`, restart `pnpm dev` (look for `[vite] Forced re-optimization of dependencies`), then user MUST hard-refresh browser to drop stale chunk URLs. Verified by build+check+SSR all passing despite error. Note: `pkill -f astro` kills the calling shell because the command line contains "astro" — use `pkill -9 -f 'astro dev'` from a separate command without "astro" in the same cmdline, or just don't combine. — Failed: Stale Vite dep cache / HMR desync caused Svelte init_operations to not run before template walk during hydration. <!-- created=2026-06-21, last=2026-06-21 -->
§
[insight] Svelte 5 + Vite/Astro hydration error "Failed to hydrate: TypeError: Cannot read properties of undefined (reading 'call')" at `get_first_child` (operations.js) → `template.js` → `hmr.js` branch → `hydrate`. Root cause is NOT a code bug: stale Vite dep cache (`node_modules/.vite/deps`) / browser-cached chunks with mismatched `?v=` hashes create DUPLICATE Svelte runtime instances. `init_operations` sets `first_child_getter` on one instance; the component template uses another instance where it stays undefined → `.call` fails. Known issue: sveltejs/svelte#15104. Common triggers: cloudflare tunnel caching, sporadic dev cache, packages that monkeypatch prototypes (axios-retry). Fix: stop dev server, `rm -rf node_modules/.vite`, restart dev, HARD-REFRESH browser (or disable cache in devtools). Code itself hydrates correctly — verify with headless chromium before chasing phantom mismatches. — Failed: Duplicate Svelte runtime module instances from stale Vite dep cache / browser cache; init_operations never sets first_child_getter on the instance the component uses. <!-- created=2026-06-21, last=2026-06-21 -->
§
[tool-quirk] Svelte comments are HTML `<!-- -->`, NOT JSX `{/* */}`. `{/* */}` causes js_parse_error in svelte compiler. Also: `npx svelte-check` installs a fresh svelte-check with its OWN bundled Svelte version that may differ from the project's — its errors can be misleading. Validate with the project's installed `svelte/compiler` (parse/compile) or `astro check`/`pnpm build` instead. <!-- created=2026-06-21, last=2026-06-21 -->
§
[tool-quirk] Exa API v2 response uses camelCase fields (e.g. `requestId`, `searchTime`, `costDollars`), not snake_case (`request_id`). Parse accordingly in agent/extensions/exa/index.ts. — Failed: Initially coded snake_case parsing; live API call returned camelCase, fixed by switching to camelCase field names. <!-- created=2026-06-22, last=2026-06-22 -->